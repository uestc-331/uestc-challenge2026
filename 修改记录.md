# 7.1
## 问题一：四足机器人在Gazebo中运行时，动作卡顿
- 不是GPU渲染的问题，是CPU的物理仿真慢。
- 修改：将 Mid360 插件的采样数从24000 调到 8000
- 修改的文件路径：/src/unitree_guide/unitree_guide/robots/a1_description/xacro/gazebo.xacro
- 修改内容：
```bash
<!-- gazebo livox mid360 plugins -->
<downsample>3</downsample>
```

## 问题二：Gazebo仿真器的 Real Time Factor 因子远小于1，只有0.13左右
- Real Time Factor = 仿真时间 / 真实时间，越接近1越好
- 依旧是CPU的物理仿真慢，当然和仿真场景的复杂度也有关
- 修改：将仿真器的真实时间更新频率下调
- 修改文件路径：/src/building_generator_core/building_generator_core/exporter.py
- 修改内容：
```bash
def _render_world_sdf(layout: BuildingLayout) -> str:
    sdf = ET.Element("sdf", {"version": SDF_VERSION})
    world = ET.SubElement(sdf, "world", {"name": "generated_world"})
    ## 修改部分 max_step_size=0.002：每次物理步进 2ms   real_time_update_rate=500：目标每秒 500 次物理更新
    physics = ET.SubElement(world, "physics", {"type": "ode"})
    ET.SubElement(physics, "max_step_size").text = "0.002"
    ET.SubElement(physics, "real_time_factor").text = "1"
    ET.SubElement(physics, "real_time_update_rate").text = "500"
    ##
    include_sun = ET.SubElement(world, "include")
```

## 问题三：四足机器人进入楼房后观察困难
- 修改：将楼房的天花板设置为半透明
- 修改文件路径：/src/building_generator_core/building_generator_core/exporter.py
- 修改内容：
```bash
函数 _append_floor_plate()，这里：
_append_box(
    model,
    name=f"slab_floor_{floor.floor_index}_{section_index}",
    size=(rect.width, rect.length, SLAB_THICKNESS),
    pose=(rect.center[0], rect.center[1], z_floor, 0.0, 0.0, 0.0),
    ## 最后一个参数是透明度  RGBA
    color="0.76 0.76 0.77 0.35",
)
```
## 问题四：四足机器人无法跟随电梯上下行
- 当前电梯不是“物理电梯”，而是“被服务瞬移的 kinematic 模型”，kinematic 物体在 Gazebo 里不按普通动力学交互，它可以被强制移动，但不会像真实平台那样通过摩擦、约束、速度把机器人稳定带上去
- 修改：调用电梯时同时移动机器人 pose
- 修改文件路径：/src/building_generator_classic/building_generator_classic/control_runtime.py
/src/building_generator_classic/building_generator_classic/control_service.py
- 添加内容：
```bash
def _carry_robot_with_elevator
```