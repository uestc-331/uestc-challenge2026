# 文档撰写辅助说明

这份 `agent.md` 不是给开发者编译和调试用的，而是专门写给项目材料撰写者、答辩整理者、创新点提炼者看的。

它的目标很明确：

- 帮助后来接手文档的人快速理解这个项目到底做了什么
- 帮助撰写者理解哪些代码和文档最值得看
- 帮助撰写者更好地理解“自主创新点提炼”这类文档
- 减少重复翻代码、重复追聊天记录的时间成本

你可以把它理解成：

`这是给文档撰写者看的项目地图`

## 1. 这个项目应该怎么定义

这个项目本质上是一个：

`面向挑战杯复杂室内多楼层场景的四足机器人自主探索系统`

它不是简单地把某个现成算法跑起来，而是把原本偏无人机的自主探索框架，改造成适合四足机器人在比赛环境里使用的系统。

从文档写作的角度，最重要的一点是：

**这个项目真正的价值，不是“把 FUEL 跑起来了”，而是“把一套原本面向无人机三维探索的框架，迁移并重构到了四足机器人、多房间、多楼层、窄门、电梯、回家返回、视觉噪声等复杂比赛环境中”。**

这句话几乎可以作为所有创新点写作的总前提。

## 2. 这个项目主要由哪些部分组成

从代码结构看，最关键的部分主要集中在下面三块：

### 2.1 FUEL 算法侧

路径：

- `src/fuel/fuel_planner`

这部分是探索、建图、frontier、A*、B 样条、状态机等核心算法所在的位置。

可以简单理解为：

`这是探索算法本体`

### 2.2 四足狗适配层

路径：

- `src/fuel/fuel_dog_adapter`

这部分是把原本偏无人机的 FUEL 接到四足狗上的关键桥梁。

可以简单理解为：

`这是算法落地到四足机器人上的适配层`

### 2.3 仿真与机器人模型侧

路径：

- `src/unitree_guide`

这部分主要是 Unitree A1 四足狗仿真、Gazebo 环境、启动文件、机器人模型等内容。

可以简单理解为：

`这是比赛环境和机器人本体的仿真基础`

## 3. 整个系统的高层工作流程

如果从功能链路上理解整个项目，可以按下面这个流程看：

1. 深度传感器、里程计、传感器位姿提供环境观测
2. FUEL 建立占据地图 / SDF / ESDF 类环境表示
3. frontier 检测找到已知与未知的边界
4. viewpoint 选择决定下一步最值得观测的位置
5. A* 先生成离散路径
6. B 样条对路径进行平滑优化
7. 四足狗适配层把规划命令变成 `/cmd_vel`
8. 机器人执行探索、重规划、回家、换层等任务

对于文档写作者来说，这条链路非常重要，因为你后面写创新点、系统框图、模块设计、任务流程，基本都绕不开这个流程。

## 4. 最值得优先阅读的关键文件

如果文档撰写者时间有限，不要一开始就全仓库乱翻。建议优先看下面这些文件。

### 4.1 关键启动文件

#### `src/fuel/fuel_dog_adapter/launch/fuel_dog_visual_exploration.launch`

这是当前单层视觉/深度探索最重要的启动文件。

它的重要性在于：

- 包含大量最终实际使用的参数
- 能看出探索范围、相机、速度、固定高度、动态 box 等设置
- 能帮助理解这个系统是如何真正被跑起来的

#### `src/fuel/fuel_dog_adapter/launch/fuel_multifloor_mission.launch`

这是多楼层任务的总启动文件。

它的重要性在于：

- 能看出电梯、多楼层、return-home、任务流程编排等内容
- 对多楼层创新点写作尤其关键

#### `src/fuel/fuel_planner/exploration_manager/launch/algorithm.xml`

这是 FUEL 侧核心算法参数集中地。

它的重要性在于：

- frontier 相关参数
- replanning 相关参数
- 感知建图参数
- 轨迹和状态机参数

很多创新点最后都会落到这个文件里的参数设计或配合逻辑上。

### 4.2 四足狗适配层脚本

#### `src/fuel/fuel_dog_adapter/scripts/fuel_poscmd_to_cmdvel.py`

这是整个“无人机探索算法迁移到四足机器人”叙事中的关键证据。

它的作用是：

- 把规划器输出的命令转成四足狗真正能执行的 `/cmd_vel`
- 把原本无人机式控制语义，转换为地面机器人能执行的速度控制语义

文档中如果要强调“算法迁移创新”，这个文件一定要提。

#### `src/fuel/fuel_dog_adapter/scripts/odom_to_sensor_pose.py`

它的作用是：

- 根据四足狗里程计生成传感器位姿
- 为视觉/深度建图提供必要输入

这个文件对解释“视觉探索如何落地”很有帮助。

#### `src/fuel/fuel_dog_adapter/scripts/dynamic_exploration_box.py`

这个文件非常重要，因为它对应了后面很多和“探索范围动态调整”有关的创新点。

它的作用是：

- 根据位置和阶段动态改变探索边界
- 解决门口、走廊、房间、回家路径中的探索范围冲突问题

#### `src/fuel/fuel_dog_adapter/scripts/fuel_multifloor_mission.py`

这个文件对应多楼层任务流程。

它的作用是：

- 管理多楼层探索任务
- 串联电梯、回家、换层、重新探索等流程

### 4.3 FUEL 算法侧关键源码

#### `src/fuel/fuel_planner/active_perception/src/frontier_finder.cpp`

这是 frontier 和 viewpoint 逻辑的重要入口。

适合用来支撑：

- frontier/viewpoint 相关说明
- 假 frontier 分析
- 为什么有些 frontier 没有 viewpoint

#### `src/fuel/fuel_planner/exploration_manager/src/fast_exploration_fsm.cpp`

这是探索状态机核心文件。

适合用来支撑：

- 重规划
- WAIT_TRIGGER / PLAN / PUB / FINISH / RETURN_HOME 等状态逻辑
- 为什么有时轨迹执行不久就重规划
- return-home 行为如何组织

#### `src/fuel/fuel_planner/exploration_manager/src/fast_exploration_manager.cpp`

这是探索流程协调的重要位置。

适合用于理解：

- exploration manager 如何组织 frontier、路径、轨迹

#### `src/fuel/fuel_planner/path_searching/src/astar.cpp`

这是 A* 路径规划核心之一。

适合支撑：

- A* 路径来源
- A* 与 B 样条之间的关系
- A* 在门口、走廊、房间中的行为分析

#### `src/fuel/fuel_planner/bspline_opt/src/bspline_optimizer.cpp`

这是 B 样条优化核心文件。

适合支撑：

- B 样条偏离
- B 样条打结
- 轨迹平滑和穿墙问题
- 优化参数设计

#### `src/fuel/fuel_planner/plan_manage/src/planner_manager.cpp`

这是规划管理的重要文件。

适合支撑：

- 局部轨迹生成
- 起点速度、轨迹起点、执行一致性相关问题

#### `src/fuel/fuel_planner/plan_manage/src/traj_server.cpp`

这是轨迹发布和采样的重要文件。

适合支撑：

- 轨迹可视化
- 期望轨迹发布
- B 样条和执行路径的关系

#### `src/fuel/fuel_planner/plan_env/src/map_ros.cpp`

这个文件适合用于解释：

- 深度图/点云如何进入 FUEL 建图链路

#### `src/fuel/fuel_planner/plan_env/src/sdf_map.cpp`

这个文件很关键，适合支撑：

- 障碍膨胀
- SDF/ESDF 环境表示
- Z 向膨胀
- 低矮障碍避障问题

## 5. 根目录里已经有的关键文档

文档撰写者在开始新写之前，建议优先复用下面这些已有文档。

### `自主创新点提炼_评估版.md`

这是当前对创新点的初步提炼稿。

作用：

- 给创新点部分提供总体包装方向
- 帮助你快速知道目前最值得写的 8 个创新点是什么

### `fuel算法改进适配问答摘录.md`

这是从历史问答里整理出的算法修改/适配相关内容摘录。

作用：

- 提供问题背景
- 提供现象和改动依据
- 可以作为创新点写作时的“证据池”

### `fuel算法改进适配问答索引.md`

这是摘录文档的主题索引。

作用：

- 快速查找某一类问题，比如 frontier、B 样条、重规划、多楼层等

### `fuel解析.md`

作用：

- 解释 FUEL 各个功能包大概是干什么的
- 适合写系统架构和算法模块说明时参考

### `fuel修改.md`

作用：

- 记录了在接入四足狗过程中具体修改了什么
- 非常适合写“我们做了哪些工程改造”

### `docs/sensors-and-topics.md`

作用：

- 解释传感器和 topic 的关系

### `docs/algorithm-interfaces.md`

作用：

- 解释模块之间怎么连起来

### `docs/evaluation.md`

作用：

- 适合写结果、评测、性能说明

### `docs/troubleshooting.md`

作用：

- 适合写“问题驱动型创新”
- 尤其适合把真实 bug、真实失败现象转化为你们的改进亮点

## 6. 文档写作时最值得抓住的三条主线

如果后续有人负责写项目书、答辩 PPT、创新总结、技术报告，最建议围绕以下三条主线展开。

### 6.1 算法迁移主线

核心说法：

`我们把原本面向无人机的三维自主探索框架，迁移并重构为适用于四足机器人的地面探索系统。`

支撑证据：

- `fuel_poscmd_to_cmdvel.py`
- 固定高度 / 2.5D 探索
- 地面机器人友好的速度与朝向处理

这条主线适合写：

- 为什么这个工作不只是“复现”
- 为什么这个系统有工程价值
- 为什么这个项目有自主创新性

### 6.2 比赛场景约束主线

核心说法：

`我们不是把探索算法丢进一个开放环境，而是围绕比赛中的门、走廊、房间、电梯、回家返回等结构化约束，对探索流程进行了重构。`

支撑证据：

- 动态探索边界
- 门区范围切换
- 回家阶段特殊处理
- 多楼层任务流程

这条主线适合写：

- 场景适配创新
- 比赛导向创新
- 系统设计特色

### 6.3 鲁棒性增强主线

核心说法：

`项目最核心的工程工作之一，不是简单调参，而是解决真实比赛环境中由传感器几何、窄门、对称结构、低矮障碍引发的不稳定问题。`

支撑证据：

- 假 frontier 分析与抑制
- 重规划时序与抖动问题
- B 样条偏离和打结问题
- Z 向障碍膨胀

这条主线适合写：

- 创新点
- 技术难点
- 工程亮点

## 7. 当前最适合提炼成创新点的主题

下面这些主题已经足够成熟，可以直接作为比赛材料里的创新点来写。

### 7.1 无人机探索算法向四足机器人平台的运动学重解释

适合写：

- 为什么迁移难
- 为什么不是简单换话题名
- 为什么要做执行机构层面的重新解释

### 7.2 探索边界动态收缩/扩展机制

适合写：

- 门口、走廊、房间过渡
- 避免过早探索无关区域
- return-home 可达性保障

### 7.3 对称门口场景下的探索决策去抖

适合写：

- 左右横跳
- 门区振荡
- 侧向偏好和阶段锁侧

### 7.4 A* 到 B 样条的轨迹可执行性校正

适合写：

- A* 可行但 B 样条失真
- 短路径、弯折路径、窄门路径的平滑偏差
- 打结问题与执行一致性问题

### 7.5 面向四足机器人的重规划节奏重构

适合写：

- 重规划太快
- 轨迹起点滞后
- 高速重规划对地面机器人不友好

### 7.6 假 frontier 抑制

适合写：

- 视觉噪声
- 穿墙感知
- raycast 误清空
- 障碍内部 frontier

### 7.7 参数化 Z 向障碍膨胀

适合写：

- 低矮障碍
- 球体、矮障碍物
- 机器人机身安全包络

### 7.8 多楼层探索-返航-换层任务闭环

适合写：

- 电梯流程
- 回家流程
- 多楼层任务编排

## 8. 文档写作时不要怎么写

文档撰写者需要特别注意下面几点：

### 8.1 不要把所有内容写成“我们调了参数”

因为这样会显得工作很弱。

更好的写法是：

- 发现了什么场景矛盾
- 原始算法为什么在这里失效
- 我们设计了什么机制
- 带来了什么收益

### 8.2 不要轻易宣称“提出了全新的基础算法”

更稳妥的说法是：

- 系统性重构
- 算法迁移
- 面向比赛场景的适配
- 鲁棒性增强
- 任务级创新

### 8.3 不要把工程适配写得太“工具化”

不要只写成：

`我们把 A*、B 样条、frontier 调了调`

更好的写法是：

`我们识别出原始 FUEL 假设与四足机器人比赛场景之间存在结构性失配，并围绕感知、规划、执行链路进行了针对性重构。`

## 9. 建议统一采用的写作句式

后续无论写创新点、技术难点、系统亮点，建议都尽量用下面这个模板：

1. 发现了什么比赛环境下的关键问题
2. 为什么原始算法在这里不适用
3. 我们提出了什么机制 / 策略 / 模块
4. 这样做带来了什么效果

也就是：

`问题驱动 -> 机制设计 -> 系统收益`

这个写法会明显比“我们做了若干修改”更像真正的自主创新。

## 10. 如果你是文档撰写者，建议怎么用这份文件

### 如果你要写项目书 / 申报材料

优先看：

- 第 6 节
- 第 7 节
- `自主创新点提炼_评估版.md`

### 如果你要写答辩 PPT

优先看：

- 第 7 节
- `自主创新点提炼_评估版.md`

然后把内容压缩成 3 到 4 个一级创新标题。

### 如果你要写技术报告

优先看：

- 第 4 节
- 第 5 节
- `fuel解析.md`
- `fuel修改.md`

### 如果你要写创新总结

优先看：

- `自主创新点提炼_评估版.md`
- `fuel算法改进适配问答索引.md`
- `fuel算法改进适配问答摘录.md`

## 11. 新接手文档的人，建议的最短阅读路径

如果时间很紧，建议按下面顺序看：

1. `自主创新点提炼_评估版.md`
2. `fuel算法改进适配问答索引.md`
3. `fuel算法改进适配问答摘录.md`
4. `fuel修改.md`
5. `fuel解析.md`
6. `docs/sensors-and-topics.md`
7. `docs/algorithm-interfaces.md`

如果时间更紧，只看下面 3 个也能先写出第一版：

1. `自主创新点提炼_评估版.md`
2. `fuel修改.md`
3. `fuel算法改进适配问答索引.md`

这通常已经足够完成第一轮创新点整理和项目理解。
