# 核心代码与运行环境说明

本文用于项目交接、文档撰写和答辩材料整理，帮助团队快速理解本项目的核心代码文件，以及当前仿真系统的运行环境。

## 1. 项目定位

本项目面向挑战杯比赛仿真环境，目标是在多楼层室内工厂/厂房场景中，使 Unitree A1 四足机器人完成自主探索、多楼层切换、危险源识别和最终返回出生点。

系统整体基于：

- ROS1 Noetic
- Gazebo Classic
- Unitree A1 四足机器人仿真与控制接口
- FUEL 自主探索框架
- RealSense 深度相机视觉建图
- 多楼层建筑生成、电梯和动态门控制服务

项目的核心工作不是简单运行原版 FUEL，而是将原本偏无人机三维探索的 FUEL 框架，适配到四足机器人、视觉深度感知、多楼层室内比赛场景中。

## 2. 核心代码文件总览

核心代码可分成 5 层：

1. 多楼层任务流程层
2. FUEL 四足狗适配层
3. FUEL 探索状态机与管理层
4. 路径规划与轨迹优化层
5. 建图与环境表示层

如果时间有限，优先阅读第 1、2、3 层。

## 3. 多楼层任务流程层

### `src/fuel/fuel_dog_adapter/scripts/fuel_multifloor_mission.py`

这是多楼层比赛任务的总控脚本，是当前项目最重要的流程代码之一。

主要功能：

- 启动每层 FUEL 探索
- 等待每层探索完成
- 监听 FUEL return-home 状态
- 控制电梯门提前打开
- 控制四足狗进出电梯
- 切换楼层
- 所有楼层完成后返回 0 楼
- 最后回到机器人出生点

关键理解：

- FUEL 只负责“当前楼层探索”和“本层探索完成后回到电梯门前”
- 多楼层、电梯、最终回出生点由该脚本负责编排

### `src/fuel/fuel_dog_adapter/launch/fuel_multifloor_mission.launch`

这是多楼层任务的参数入口。

主要参数：

- `elevator_inside_x/y/yaw`：进入电梯内部的目标点
- `floor_exit_x/y/yaw`：每层出电梯后开始探索的位置
- `floor0_final_x/y/yaw`：所有楼层结束后，0 楼出电梯门后的中间点
- `go_final_home`：是否最终回出生点
- `final_home_x/y/yaw`：最终出生点目标
- `max_linear_speed`、`max_angular_speed`：多楼层任务中直接导航的速度限制

当前默认机器人出生点：

```text
x = 0.0
y = -2.2
yaw = 1.5708
```

该值来自当前生成场景的 `generated_building/scene_manifest.json`。

## 4. FUEL 四足狗适配层

### `src/fuel/fuel_dog_adapter/scripts/fuel_poscmd_to_cmdvel.py`

这是 FUEL 接入四足狗的核心适配器。

原始 FUEL / traj_server 输出：

```text
/position_cmd
quadrotor_msgs/PositionCommand
```

四足狗控制器需要：

```text
/cmd_vel
geometry_msgs/Twist
```

该脚本的作用就是把无人机式位置命令转换为四足狗可执行的速度命令。

核心逻辑：

- 读取 FUEL 期望位置、速度、加速度和 yaw
- 使用位置误差 P 项、速度误差 D 项、速度前馈和加速度前馈生成世界系速度意图
- 根据当前 odom yaw 转换到机器人机体系
- 根据四足机器人特性限制横移、限速、限加速度
- 控制 yaw，使机器人尽量朝向期望运动方向
- 在命令或 odom 超时时自动停车

该文件支撑的创新点：

- 无人机探索算法向四足机器人平台的运动学重解释
- 世界系规划命令到机体系速度控制的适配
- 低 real time factor 下的控制稳定性调整

### `src/fuel/fuel_dog_adapter/scripts/odom_to_sensor_pose.py`

这是视觉/深度建图的关键适配节点。

FUEL 建图需要传感器在 `world` 坐标系下的位姿，而仿真直接提供的是四足狗 odom。

该脚本的作用：

- 订阅 `/Odometry_gazebo`
- 根据相机安装偏移计算 RealSense 深度相机位置
- 可选转换成相机 optical frame
- 发布 `/fuel_dog_adapter/sensor_pose`

关键点：

- 四足狗 base 坐标通常是 `x 前、y 左、z 上`
- 深度相机 optical frame 通常是 `x 右、y 下、z 前`
- 这两者需要固定旋转转换，否则深度投影方向会错

### `src/fuel/fuel_dog_adapter/scripts/dynamic_exploration_box.py`

这是动态探索范围控制节点。

它解决的问题：

- 门外阶段不希望过早探索左右区域
- 进入厂房/房间后需要扩大探索范围
- return-home 时如果过早恢复窄范围，A* 可能无法规划回电梯门前

核心逻辑：

- 进门前：保持窄 x 范围
- 机器人穿过指定 y 位置后：扩大 x 范围
- 可单独设置扩大后的 y 范围下界
- FUEL 进入 return-home 后：临时恢复完整 y 范围和宽 x 范围，保证回家路径可达
- 机器人回到门洞区域后：恢复初始窄范围

该文件支撑的创新点：

- 探索边界动态收缩/扩展
- 面向门区、走廊、房间结构的任务感知探索范围控制

### `src/fuel/fuel_dog_adapter/launch/fuel_dog_visual_exploration.launch`

这是单层 FUEL 视觉/深度探索主启动文件。

主要功能：

- 启动 FUEL 算法
- 启动 `traj_server`
- 启动 `odom_to_sensor_pose.py`
- 启动 `fuel_poscmd_to_cmdvel.py`
- 启动 `dynamic_exploration_box.py`
- 配置地图范围、frontier 参数、B 样条参数、速度限制、return-home 目标

关键参数类型：

- 地图大小：`map_size_x/y/z`
- 探索 box：`box_min_*`、`box_max_*`
- 动态 box：`dynamic_box_*`
- 相机内参：`cx/cy/fx/fy`
- 速度限制：`max_vel`、`max_acc`、`max_angular_speed`
- frontier 参数：`frontier_*`
- 单层 return-home：`return_home_x/y/z/yaw`
- 四足狗跟踪参数：`position_kp`、`position_kd`、`yaw_kp`、`lookahead_time`

注意：

`return_home_x/y/z/yaw` 是“每层探索完回到电梯门前”的目标，不是整场任务最终回出生点。最终回出生点由 `fuel_multifloor_mission.py` 处理。

### `scripts/send_waypoint_and_move.sh`

这是用于手动触发 FUEL 开始规划/探索的辅助脚本。

主要作用：

- 向 `/waypoint_generator/waypoints` 发布一个 `nav_msgs/Path`，让 FUEL 从 `WAIT_TRIGGER` 状态进入规划流程
- 等待 2 秒
- 向 `/cmd_vel` 发布短时间前进速度，帮助机器人完成初始前向观测和局部地图连通
- 最后发布零速度停车

这个脚本主要用于调试或单独启动 FUEL 时快速触发探索。多楼层任务正常运行时，`fuel_multifloor_mission.py` 会自动启动和触发每层探索，不一定需要手动运行该脚本。

使用方式：

```bash
cd /home/uestc/tzb/uestc-challenge2026
./scripts/send_waypoint_and_move.sh
```

注意：

- 这个脚本会直接发布 `/cmd_vel`
- 如果多楼层任务或其他控制节点正在控制机器人，不建议同时手动运行，避免控制命令冲突
- 其中的 waypoint 点本身不是最终探索目标，而是 FUEL 的启动触发信号

## 5. FUEL 探索状态机与管理层

### `src/fuel/fuel_planner/exploration_manager/src/fast_exploration_fsm.cpp`

这是 FUEL 探索状态机核心。

主要状态包括：

- `WAIT_TRIGGER`
- `PLAN_TRAJ`
- `PUB_TRAJ`
- `EXEC_TRAJ`
- `RETURN_HOME`
- `PUB_RETURN`
- `EXEC_RETURN`
- `FINISH`

关键逻辑：

- 收到 trigger 后开始探索
- 调用 exploration planner 生成轨迹
- 执行轨迹过程中根据阈值重规划
- 没有 frontier 时判断是否进入 return-home
- return-home 完成后发布 finish 事件

该文件支撑的创新点：

- 重规划机制调整
- 单层探索完成后自动返回电梯门前
- 多楼层任务与 FUEL 状态机解耦

### `src/fuel/fuel_planner/exploration_manager/src/fast_exploration_manager.cpp`

这是 FUEL 探索流程组织者。

主要功能：

- 管理 frontier 更新
- 管理 viewpoint 选择
- 组织探索目标
- 调用路径规划与轨迹生成
- return-home 时直接规划回固定 home 点

特别关注：

- `planReturnHome(...)`

该函数不再做 frontier/viewpoint 选择，而是直接从当前位置用 A* 搜索到 home 点，再生成 B 样条轨迹。

## 6. 路径规划与轨迹优化层

### `src/fuel/fuel_planner/path_searching/src/astar.cpp`

这是 A* 路径搜索核心。

作用：

- 在占据地图 / SDF 环境上搜索离散路径
- 给 B 样条优化提供初始路径
- return-home 也依赖该 A* 搜索路径

与四足狗相关的问题：

- 原始 FUEL 偏 3D 空间搜索
- 四足狗需要更接近 2.5D / 固定高度的路径
- 如果探索 box 设置不合理，A* 会出现无路可走

### `src/fuel/fuel_planner/plan_manage/src/planner_manager.cpp`

这是规划管理核心。

关键函数：

- `planExploreTraj(...)`

主要作用：

- 接收 A* / frontier tour 生成的离散路径点
- 先生成多项式轨迹
- 再参数化为 B 样条控制点
- 调用 B 样条优化器
- 钉住起点、终点和初始速度

该文件支撑的创新点：

- A* 离散路径到 B 样条连续轨迹的可执行性校正
- B 样条起点速度和四足狗当前速度一致性验证

### `src/fuel/fuel_planner/bspline_opt/src/bspline_optimizer.cpp`

这是 B 样条优化器。

作用：

- 根据平滑代价、障碍代价、时间代价等优化控制点
- 让离散路径变成连续可执行轨迹

与本项目相关的问题：

- 短而弯折的 A* 路径容易被 B 样条过度平滑
- 门口、窄通道容易出现轨迹偏离或打结
- 因此需要控制点密度、局部段长、边界状态和障碍代价共同约束

### `src/fuel/fuel_planner/bspline/src/non_uniform_bspline.cpp`

这是 B 样条数学表示和采样实现。

作用：

- 表示非均匀 B 样条
- 求导得到速度/加速度
- 按时间采样轨迹点

## 7. Frontier / Viewpoint 层

### `src/fuel/fuel_planner/active_perception/src/frontier_finder.cpp`

这是 frontier 检测和 viewpoint 生成的核心。

主要功能：

- 在地图中寻找已知自由空间与未知空间的边界
- 对 frontier 进行聚类和分裂
- 为每个 frontier 采样候选 viewpoint
- 计算每个 viewpoint 能看到多少 frontier cell
- 没有 viewpoint 的 frontier 放入 dormant 列表

该文件支撑的创新点：

- frontier/viewpoint 机制解释
- 假 frontier 分析
- 障碍内部 frontier 问题
- viewpoint 不在当前探索 box 时的过滤

## 8. 建图与环境表示层

### `src/fuel/fuel_planner/plan_env/src/map_ros.cpp`

这是传感器输入进入 FUEL 地图模块的接口。

作用：

- 接收深度图或点云
- 接收传感器 pose
- 将观测数据送入 SDF map 更新流程

当前视觉探索主要使用：

```text
/real_sense/depth/image_raw
/fuel_dog_adapter/sensor_pose
```

### `src/fuel/fuel_planner/plan_env/src/sdf_map.cpp`

这是占据地图、SDF/ESDF 和障碍膨胀相关核心。

作用：

- 维护占据栅格
- 维护已知/未知/障碍状态
- 进行障碍膨胀
- 支持参数化 Z 向障碍膨胀

该文件支撑的创新点：

- 低矮障碍避障
- 参数化 Z 向障碍膨胀
- 四足机器人安全包络建模

### `src/fuel/fuel_planner/plan_env/src/raycast.cpp`

这是 raycast 射线更新相关代码。

作用：

- 深度图建图时，根据相机到深度点之间的射线清空自由空间
- 帮助构建已知 free / occupied / unknown 状态

与本项目相关的问题：

- 深度噪声或穿墙感知可能导致障碍物后方被错误清成 free
- 这会诱发假 frontier

## 9. 运行环境

当前本机环境检测结果：

```text
Ubuntu: 20.04.6 LTS
ROS: Noetic
Python: 3.8.10
Gazebo: 11.15.1
```

项目推荐运行环境：

- Ubuntu 20.04
- ROS Noetic
- Gazebo Classic 11
- Python 3.8+
- catkin 工作空间
- Unitree A1 仿真与 `junior_ctrl`
- `numpy`
- `scipy`
- `python3-yaml`
- CUDA >= 11.7
- libtorch C++ 版本，用于 Unitree A1 控制器

## 10. 编译与启动

### 编译

```bash
cd /home/uestc/tzb/uestc-challenge2026
source /opt/ros/noetic/setup.bash
catkin_make -j
source devel/setup.bash
```

### 启动比赛仿真环境

```bash
cd /home/uestc/tzb/uestc-challenge2026
source devel/setup.bash
./auto.sh
```

`auto.sh` 会完成：

1. 清理旧进程
2. 生成随机多楼层场景
3. 启动 Gazebo
4. 加载 Unitree A1
5. 启动传感器
6. 启动门/电梯控制服务
7. 启动四足狗控制器

常用参数：

```bash
SEED=77 ./auto.sh
GUI=false ./auto.sh
FLOOR_COUNT=2 ./auto.sh
```

### 启动多楼层 FUEL 任务

```bash
source devel/setup.bash
roslaunch fuel_dog_adapter fuel_multifloor_mission.launch
```

多楼层任务启动后，系统会：

1. 启动当前楼层 FUEL 探索
2. 当前楼层探索完成后回电梯门前
3. 进入电梯
4. 上一层
5. 出电梯后重新启动 FUEL
6. 所有楼层结束后回到 0 楼
7. 出电梯后回到机器人出生点

### 手动触发单层 FUEL 探索

如果只启动了单层 FUEL，或者需要手动让 FUEL 从等待触发状态进入规划，可以运行：

```bash
./scripts/send_waypoint_and_move.sh
```

该脚本会向 `/waypoint_generator/waypoints` 发布触发消息，并短时间发布前进 `/cmd_vel`。它更适合调试单层探索，不是多楼层任务的必要步骤。

## 11. 关键 ROS 话题

| 话题 | 类型 | 说明 |
|------|------|------|
| `/cmd_vel` | `geometry_msgs/Twist` | 四足狗速度控制输入 |
| `/Odometry_gazebo` | `nav_msgs/Odometry` | 仿真里程计 |
| `/waypoint_generator/waypoints` | `nav_msgs/Path` | FUEL 开始探索的触发话题 |
| `/position_cmd` | `quadrotor_msgs/PositionCommand` | FUEL/traj_server 输出的轨迹命令 |
| `/real_sense/depth/image_raw` | `sensor_msgs/Image` | RealSense 深度图 |
| `/real_sense/rgb/image_raw` | `sensor_msgs/Image` | RealSense RGB 图像 |
| `/real_sense/rgb/camera_info` | `sensor_msgs/CameraInfo` | RGB 相机内参 |
| `/fuel_dog_adapter/sensor_pose` | `geometry_msgs/PoseStamped` | 给 FUEL 建图使用的传感器位姿 |
| `/planning/bspline` | `bspline/Bspline` | FUEL 规划出的 B 样条轨迹 |
| `/planning/finish` | `std_msgs/Empty` | 当前楼层探索/回家完成事件 |
| `/planning/return_home_start` | `std_msgs/Empty` | FUEL 开始 return-home 的事件 |
| `/vision/danger_candidates` | `visualization_msgs/MarkerArray` | 视觉检测危险源候选点可视化 |

## 12. 结果文件

主要结果文件：

```text
results/detected_danger.json
results/evaluation_result.json
results/danger_truth.json
```

说明：

- `danger_truth.json` 是真值，比赛算法不应读取
- `detected_danger.json` 是算法输出
- `evaluation_result.json` 是评估结果

## 13. 写文档时建议引用的已有材料

建议优先阅读：

- `agent.md`
- `自主创新点提炼_评估版.md`
- `fuel算法改进适配问答索引.md`
- `fuel算法改进适配问答摘录.md`
- `fuel修改.md`
- `fuel解析.md`
- `docs/quick-start.md`
- `docs/sensors-and-topics.md`
- `docs/algorithm-interfaces.md`
- `docs/evaluation.md`

## 14. 一句话总结

本项目的核心代码围绕一条主链路展开：

```text
视觉/深度建图 -> frontier/viewpoint -> A* -> B 样条 -> /position_cmd -> /cmd_vel -> 四足狗执行 -> 多楼层任务状态机
```

理解这条链路，就基本能理解整个项目的技术主线和创新点来源。
