# 自主创新点提炼（评估版）

先给一个总判断：

**你们这个项目真正有价值的，不是“把 FUEL 跑起来了”，而是把一套原本面向无人机三维探索的算法，重构适配到了四足机器人、多房间、多楼层、窄门、回家返回、视觉感知不稳定的比赛环境里。** 这类工作非常适合提炼成“系统级创新”，因为它不是单点算法，而是把感知、建图、frontier、轨迹优化、运动约束、任务流程串成了一个能落地的完整闭环。

## 一条总的创新主线

你们可以把项目定义为：

**“面向挑战杯复杂室内多楼层场景的四足机器人自主探索系统”**

核心创新不是单一模块，而是三层：

1. **算法迁移创新**  
   把原本用于无人机的 FUEL 三维探索框架，迁移为适用于四足机器人的受限运动探索框架。

2. **场景约束创新**  
   针对比赛环境里的房门、走廊、电梯、多楼层、回家返回等任务特征，做了探索范围和任务流程的动态重构。

3. **工程鲁棒性创新**  
   针对视觉噪声、假 frontier、轨迹打结、重规划抖动、低矮障碍漏检等真实问题，做了系统级补偿和稳定化设计。

## 建议重点写的 8 个创新点

### 1. 无人机探索算法向四足机器人平台的“运动学重解释”
**一句话版本：**  
我们不是简单复用 FUEL，而是完成了从“全向三维飞行器”到“地面四足机器人”的运动学重定义。

**可以怎么讲：**
- 原始 FUEL 默认是无人机：轨迹有高度自由度，控制量是位置/速度/yaw，默认可以在三维空间灵活穿行。
- 四足狗不是这个模型：它有明显的平面运动约束、转向代价、不能接受过强横移、对倒退和急转更敏感。
- 你们做的不是“换个 topic 名字”，而是把：
  - 三维探索逻辑压缩到 **2.5D 地面探索**
  - 世界系位置/速度命令转成 **机体系速度控制**
  - 高度自由度约束成 **固定行走层**
  - 无人机式轨迹平滑改造成 **地面机器人可执行轨迹**

**亮点词：**
- “运动学重解释”
- “从飞行器假设转换到足式机器人假设”
- “探索算法与执行机构解耦重构”

### 2. 提出了“探索边界动态收缩/扩展”的任务约束机制
**一句话版本：**  
我们没有把探索范围写死，而是根据机器人位置和任务阶段，动态切换可探索空间，解决门区误探索和返航失败问题。

**你们这个点其实很能打：**
- 在房门、走廊、房间这种结构化场景里，静态 box 很容易出问题：
  - 过早探索两侧无关区域
  - 回房时误删/误选 viewpoint
  - return-home 时 A* 无法生成路径
- 你们做的是一套 **动态探索边界管理机制**：
  - 通过门后扩大 x 范围
  - 同时按阶段收缩 y 范围
  - 回家阶段再恢复范围
  - 把“探索边界”变成任务流程的一部分，而不是固定参数

**这可以包装成：**
- “任务感知的探索空间裁剪”
- “分阶段 frontier 可达域控制”
- “面向多房间场景的动态搜索域重配置”

### 3. 针对对称门口场景，提出了“探索决策去抖”思路
**一句话版本：**  
针对左右对称门口导致的 frontier/viewpoint 横跳问题，我们从重规划频率、侧向偏好、阶段锁侧等角度设计了稳定化机制。

**这类问题非常像真实机器人竞赛问题：**
- 理论上左右都能进，算法就会来回切换最优解
- 实际上四足狗一旦左右抖动，就会浪费时间，甚至卡在门口
- 你们已经围绕这个问题做了很多分析：
  - 降低重规划频率
  - 左侧软偏好
  - 进门阶段锁侧
  - 限制倒退与绕圈

**可写成创新：**
- “针对对称结构环境的探索决策去抖机制”
- “门区场景下的 frontier 选择稳定化”
- “减少局部最优切换带来的行为振荡”

### 4. 针对 B 样条在地面窄空间中的失真，建立了“路径可执行性校正链”
**一句话版本：**  
我们发现 A* 可行不代表 B 样条可执行，因此建立了从 A* 到 B 样条再到四足执行器的全链路校正机制。

**这是非常好的创新点。**
因为很多队伍只会说“我们用了 B 样条优化”，但你们已经碰到了真实问题：
- A* 轨迹短且弯折时，B 样条偏离大
- 门口短路径会被过度平滑
- 有时出现“打结”轨迹
- B 样条起点和实际执行位置不一致

**你们可总结成三件事：**
1. 识别出 **离散可达 ≠ 连续可执行**
2. 分析出 B 样条偏差与控制点密度、局部段长、起点速度、重规划时序相关
3. 对起点速度一致性、终点贴合、局部段参数进行了修正和验证

**包装说法：**
- “面向四足机器人窄通道场景的 B 样条可执行性校正”
- “离散规划到连续轨迹之间的行为一致性补偿”
- “轨迹优化结果与足式执行器约束的联合适配”

### 5. 设计了“重规划鲁棒化”机制，解决规划过快、轨迹滞后和行为抖动
**一句话版本：**  
我们把原本偏无人机风格的高频重规划，改造成更适合地面机器人的节制型重规划。

**为什么这个点好：**
- 无人机能吃高频 replan，四足狗不一定行
- 你们已经发现：
  - 轨迹还没执行多少就重规划
  - 红色 B 样条起点总落在实际轨迹后方
  - 门口区域抖动严重
- 你们不是只改一个参数，而是在理解 FSM 触发条件后系统调优：
  - `thresh_replan1/2/3`
  - `replan_time`
  - 规划起点与执行起点时序差
  - 当前速度与新轨迹初始速度一致性

**这个点可以很亮：**
- “从高频局部最优到稳定可执行的重规划策略重构”
- “针对足式平台的重规划节奏重设计”
- “通过时序分析解决规划起点滞后问题”

### 6. 提出了“假 frontier 抑制”方案，面向视觉噪声和穿墙感知
**一句话版本：**  
我们没有把假 frontier 当成单纯参数问题，而是从深度感知噪声、raycast 误清空和障碍邻域过滤三个层面构建了抑制机制。

**这个点特别像“科研味创新”。**
你们已经分析出问题根因：
- 深度相机靠墙时会穿透/漏深度
- raycast 把障碍后方误清成 free
- 障碍内部会冒出 frontier，但机器人永远看不掉

**所以你们的创新表述可以是：**
- 针对视觉探索中“伪未知边界”的生成机制做了分析
- 通过深度过滤、障碍比例约束、障碍净空约束等手段抑制伪 frontier
- 减少了“不可观测 frontier 持续吸引规划器”的问题

**亮点词：**
- “伪 frontier 抑制”
- “穿墙感知导致的错误 frontier 修复”
- “感知误差向决策层传播的阻断机制”

### 7. 提出“参数化 Z 向障碍膨胀”，把低矮障碍纳入地面机器人安全模型
**一句话版本：**  
针对球体、矮障碍、低台阶等目标，提出了参数化 Z 向膨胀，使原本偏二维的碰撞判断更符合四足机器人实体包络。

**这是一个很像自主创新的点。**
因为原系统更偏空中平台，默认对低矮障碍不够敏感。  
你们做的是：
- 不只在 XY 平面膨胀
- 增加 Z 向障碍膨胀
- 让机器人对“高度不高但会撞到机身/腿部”的目标更敏感

**可以写成：**
- “面向足式平台的三维安全包络重建”
- “参数化 Z 向障碍膨胀”
- “针对低矮障碍漏避障问题的碰撞模型增强”

### 8. 构建了“多楼层探索-返航-换层”的任务闭环
**一句话版本：**  
我们不是单层探索，而是把探索、返航、电梯切换、二次探索组织成了完整任务流。

**这个是系统层很加分的点。**
因为很多队伍只能做“局部自主探索”，你们做的是：
- 一层探索完成
- return-home 回到电梯口
- 电梯切层
- 指定位置再启动下一层探索
- 处理探索范围恢复和回家路径冲突

**这个可以作为压轴创新写：**
- “面向比赛环境的多楼层自主探索任务编排”
- “探索算法与任务状态机深度耦合”
- “从局部规划到全流程任务自治的扩展”

## 适合写进材料里的“总创新表述”

你可以直接用这一段：

> 本项目的创新不在于单独提出某一个全新的基础算法，而在于针对挑战杯复杂室内多楼层比赛环境，将原本面向无人机的 FUEL 三维自主探索框架，系统性重构为适用于四足机器人的地面自主探索系统。我们围绕足式平台运动约束、门区对称场景抖动、B 样条窄空间失真、视觉噪声诱发假 frontier、低矮障碍漏避障以及多楼层返航换层等关键问题，形成了一套从感知建图、frontier 生成、轨迹优化到任务状态机的完整创新链路，使算法从“能运行”提升到“能稳定完成比赛任务”。

## 如果要做成答辩 PPT，我建议最后落成 4 个一级创新标题

1. **异构平台迁移创新**  
   无人机探索算法到四足机器人平台的系统性迁移

2. **复杂场景决策创新**  
   面向门区、走廊、房间、电梯的动态探索边界与决策稳定化

3. **轨迹执行一致性创新**  
   A* - B 样条 - 四足执行器之间的可执行性校正

4. **鲁棒感知安全创新**  
   假 frontier 抑制 + Z 向障碍膨胀 + 多楼层任务闭环

## 一个很实用的建议

你们写“创新点”时，不要老写“我们调了参数”。  
要统一改写成下面这种句式：

- 发现了什么比赛环境下的关键矛盾
- 为什么原始算法在这里失效
- 我们提出了什么机制/策略/框架
- 它带来了什么效果

也就是：

> **问题驱动 -> 机制设计 -> 系统收益**

这样听起来就不是“调参”，而是“自主创新”。
